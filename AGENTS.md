# Agent Handoff Notes

- Work on branch `dev` unless the user explicitly says otherwise.
- After meaningful completed changes, run `npm test`, `npm run lint`, and `npm run build`.
- Push completed work to `origin/dev` when the user asks, or when a task explicitly includes delivery/push.
- Keep documentation honest: do not list planned features as implemented.
- User prefers Chinese responses and direct status summaries.
- User dislikes cluttered/ambiguous button flows; prefer fewer, clearer controls.
- See `docs/USER_MANUAL.zh-CN.md` for current user-facing behavior.
- See `docs/DEVELOPMENT_STATUS.md` for implemented vs pending features.
