# Cube Code 未完成事项 / 待开发清单

> 更新时间：2026-06-03  
> 当前分支：`dev`  
> 说明：本清单只记录当前代码尚未完成或需要继续完善的内容；已实现功能见 `docs/DEVELOPMENT_STATUS.md`。

## P0：影响核心使用体验，优先处理

### 1. 二进制文件模式 `0x01`

当前状态：未实现。

需要实现：

- 文件上传入口。
- 文件名、MIME type、文件大小元数据。
- 文件内容分片编码到 6 面。
- 解码后还原文件并下载。
- 文件容量限制和友好错误提示。
- 二进制 payload 的 CRC 校验。

建议测试：

- 小文本文件 round-trip。
- 图片文件 round-trip。
- 文件名含中文/空格。
- 超容量文件提示。
- 篡改分片后 CRC 报错。

---

### 2. 容量提示精确化

当前状态：已基础实现。

已实现：

- 输入时先显示近似容量，再懒加载 `qrcode` 做实际生成检查。
- 编码前会再次用 `qrcode` 实际能力预检查，超容量时阻止生成。
- 会根据当前设置提示：移除中心图标、降低纠错级别、缩短内容、改用短链接/URL。

还可继续完善：

- 给出更接近“还可输入多少字节”的精确剩余容量。
- 针对不同模式显示更详细的容量解释。
- 未来二进制/压缩模式加入后重新计算容量模型。

---

### 3. 扫描稳定性增强

当前状态：基础可用。

当前问题：

- 快速扫描只是区域扫描。
- 上传十字图主要依赖固定网格布局。
- 对倾斜、透视、模糊、低光图片支持有限。

需要实现：

- 任意角度多 QR 自动定位。
- 透视校正。
- 图片预处理：灰度、对比度增强、降噪。
- 更稳定的多二维码识别流程。
- 扫描失败时给用户明确建议。

建议测试：

- 倾斜照片。
- 远距离照片。
- 低光照片。
- 多种展开图带边距/裁切。
- 6 个二维码顺序混乱仍能重组。
- 新增展开图布局后补充对应上传识别样例。

---

## P1：重要增强

### 4. URL / Deep Link 安全 UX

当前状态：基础实现。

已支持：

- `http://`
- `https://`
- `mailto:`
- `tel:`
- `myapp://...`
- `intent://...`
- 其他安全 scheme。

已阻止：

- `javascript:`
- `data:`
- `vbscript:`
- `file:`
- `blob:`
- `about:`
- `chrome:`
- `edge:`
- `devtools:`

已完善：

- 打开外部链接/Deep Link 前会弹出确认框。
- 显示目标 scheme / host。
- 对未知 app scheme 给出额外提示。
- Android `intent://` 可解析并显示 fallback URL。

还需要完善：

- 可配置 app scheme 白名单。
- 对 fallback URL 做一键改开网页 fallback。
- iOS 不计划实现，但 Web/PWA 上仍需正常显示 deep link。

---

### 5. PWA 更新提示和离线体验

当前状态：`vite-plugin-pwa` 已接入，Service Worker 自动生成；离线 fallback 页面和应用内离线提示条已实现。

还需要实现：

- “发现新版本，点击刷新”提示。
- 更完整的离线可用说明。
- 针对不同移动浏览器继续验证离线状态探测表现。
- 更新失败/缓存异常时的恢复入口。
- 手动清理缓存/重载 PWA 的入口。

建议体验：

- 用户打开旧版本时，顶部出现轻量提示条。
- 点击后刷新并加载最新资源。

---

### 6. 普通二维码模式体验完善

当前状态：普通 QR 摄像头扫描、上传图片扫描、模式说明、结果复制已实现。

还需要完善：

- 上传普通 QR 失败时可继续增强图片预处理和更细提示。
- 普通二维码结果可考虑增加历史记录/多次扫码列表。

---

### 7. UI/交互继续打磨

当前状态：已从原生/Linux 风格升级为卡片、渐变、圆角 UI；解码页已改成魔方码/普通二维码 segmented control；摄像头错误和复制结果已使用 toast；移动端高度已做一轮压缩。

还可继续优化：

- 生成后操作区再简化。
- 扫描状态显示更明显。
- 3D 视图加载中状态。
- 错误提示进一步统一成全局 toast/snackbar。

---

## P2：测试、性能、工程化

### 8. 真实 QR 图片测试

当前状态：已新增基础覆盖。

已覆盖：

- 生成真实 QR 图像后用 jsQR 解码并重组魔方码。
- 普通二维码上传路径 `scanPlain`。
- URL/deep link 二维码。
- 带中心图标的 QR 可扫描性。
- 当前所有颜色方案：炫彩、反色、异色炫彩、基因码三色。

还可继续补充：

- 不同纠错级别的矩阵测试。
- 模糊、缩放、低光、拍照噪声场景。
- 真实导出的多种展开图整图上传样例。

---

### 9. UI 自动化测试

当前状态：未覆盖。

可选方案：

- Playwright。
- Vitest + jsdom 只做轻量 DOM 行为测试。

建议覆盖：

- 编码按钮。
- 生成选项展开/收起。
- 独立模式切换。
- 普通二维码模式切换。
- URL 解码结果按钮。

---

### 10. Android 真机测试清单

当前状态：未系统化。

需要验证：

- 摄像头权限申请。
- 扫码稳定性。
- 长按保存图片到相册。
- Android 10+ MediaStore 保存。
- Android 9 及以下存储权限。
- Deep link 跳转外部 App。
- PWA/Capacitor 资源同步。
- PWA 安装后首次启动和离线启动行为。

---

### 11. 包体积优化

当前状态：已完成一轮拆包。

已完成：

- 首屏主入口从约 737 kB 降到约 27 kB。
- `qrcode` 点击生成时按需加载。
- `jsQR` 进入扫码/上传识别时按需加载。
- Three.js 打开 3D 视图时按需加载。
- vendor chunk 已拆分为 `qrcode`、`jsQR`、`three-core`、`three-addons` 等。

还可继续优化：

- Three.js core 仍约 520 kB；可考虑替换为轻量 3D/CSS 方案。
- PWA 可考虑不预缓存大型懒加载 chunk，改为运行时缓存。
- 扫码页继续按 plain/cross/camera 细拆。

---

### 12. PWA 缓存高级策略

当前状态：Workbox generateSW 可用。

还可完善：

- 更明确的 HTML / assets / images 缓存策略文档。
- 缓存清理 UI。
- 版本号展示。
- 用户手动检查更新。

---

## 已明确不做

### iOS 原生 App

当前决定：不计划实现。

说明：

- README 里已删除 iOS 计划项。
- Web/PWA 仍可在 iOS 浏览器里访问，但不开发 iOS 原生工程。

---

## 建议开发顺序

1. 二进制文件模式 `0x01`。
2. 扫描稳定性增强：透视、低光、模糊、多 QR 定位。
3. PWA 更新提示和缓存清理入口。
4. UI 自动化测试。
5. Android 真机系统化验证。
6. URL / Deep Link scheme 白名单和 fallback 一键打开。
7. 进一步瘦身 Three.js 或替换轻量 3D 方案。
