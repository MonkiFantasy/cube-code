# Cube Code - 魔方码

一个 3D 二维码系统，将数据分布在立方体的六个面上，提供约 6 倍于标准二维码的数据容量。

## 概念

传统二维码是 2D 的 — 数据编码在单一平面上。魔方码将数据分布在立方体的六个面上：

- 每个面承载一个标准二维码，包含 **总数据的 1/6**
- **3-bit 标志位**（001–110）标识每个面的位置
- 每个二维码中心嵌入 **可读数字**（1–6），方便肉眼识别
- 扫描全部六个面后，按面 ID 排序拼接还原完整数据

```
┌───────┐ ┌───────┐ ┌───────┐
│ ┌───┐ │ │ ┌───┐ │ │ ┌───┐ │
│ │ 1 │ │ │ │ 2 │ │ │ │ 3 │ │
│ └───┘ │ │ └───┘ │ │ └───┘ │
└───────┘ └───────┘ └───────┘
┌───────┐ ┌───────┐ ┌───────┐
│ ┌───┐ │ │ ┌───┐ │ │ ┌───┐ │
│ │ 4 │ │ │ │ 5 │ │ │ │ 6 │ │
│ └───┘ │ │ └───┘ │ │ └───┘ │
└───────┘ └───────┘ └───────┘
```

## 数据容量

| 模式       | 单个二维码 (V40) | 魔方码 (6 面) |
|------------|-----------------|---------------|
| 数字模式   | 7,089 字符      | ~42,534 字符  |
| 字母模式   | 4,296 字符      | ~25,776 字符  |
| 字节模式   | 2,953 字节      | ~17,718 字节  |

## 协议

每个面的二维码载荷：

```
[3-bit 面ID] + [数据分片]
```

扫描全部 6 个面并按面 ID 排序后，完整数据结构为：

```
[版本号][数据类型][内容][CRC16校验]
```

| 字段     | 大小    | 说明                              |
|----------|---------|-----------------------------------|
| 面 ID    | 3 bit   | 001–110，标识面的位置              |
| 版本号   | 1 字节  | 协议版本                          |
| 数据类型 | 1 字节  | 0x00=文本, 0x01=二进制, 0x02=URL  |
| 内容     | N 字节  | 实际数据，拆分到 6 个面            |
| CRC16    | 2 字节  | 完整数据的校验和                   |

## 应用场景

- **产品防伪** — 3D 结构极难复制
- **工业零件标识** — 6 倍容量存储序列号、参数、批次信息
- **离线数据传输** — 通过实体魔方传递 ~17KB 数据，无需网络
- **密钥分发** — 密钥片段分布在 6 个面，需要全部扫描才能还原
- **AR 入口** — 每个面存储不同 AR 场景的入口数据

## 开发

```bash
# 安装依赖
npm install

# 启动开发服务器（支持 HTTPS，可调用摄像头）
npm run dev

# 运行测试
npm test

# 代码检查
npm run lint

# 构建
npm run build
```

浏览器打开 https://localhost:5173（自签名证书，需手动信任）。

## 技术栈

- Vanilla HTML/CSS/JS — 零框架开销
- [qrcode](https://www.npmjs.com/package/qrcode) — 二维码生成
- [jsQR](https://www.npmjs.com/package/jsqr) — 摄像头扫描解码
- [Vite](https://vite.dev/) — 开发服务器和构建工具
- [Vitest](https://vitest.dev/) — 单元测试

## 协议设计原则

- **3-bit 标志位**是信息论下限，不多不少
- 版本号、数据类型、校验码属于**数据层**，不塞进标志位
- 中心数字是**人机双重识别**，机器靠标志位，人眼靠数字
- QR 码内置 Reed-Solomon 纠错覆盖中心遮挡区域

## 许可证

MIT
